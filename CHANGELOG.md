# 更新记录

## 0.9.24 - 2026-09-08

- 抓包时间线新增逐击还原：唯一 GE 在玩家填写技能等级后直接套用对应单段倍率，并按当前单人伤害面板、全部未挂起 Buff、自动叠层最高层或固定层计算非暴击与暴击候选。
- 每一击显示理论精确值、游戏显示值、与抓包实测的最小误差，并明确标记是否落在 ±20 汇报范围内；覆纹开启时同时给出独立覆纹候选，沿用 DBF 已验证的 `K(H;r,b)=[C(H)-1]+r×C(H)×(1+b)`，不采用第三方项目的不同公式。
- 时间线增加“GE 精确 / GA 结构 / 人工映射 / 待识别”证据等级，以及只选可还原、全选 1 Hit、取消全选和按当前面板重算操作。
- 逐击证据新增稳定的 `record + sequence + primary/follow-up` 键；同一 sequence 的主伤害与后续伤害会显示配对身份，Core 重复分页或停止排空不再重复录入同一击。
- 为后续跨角色机制预留动作角色、倍率定义角色、面板角色、技能等级角色和伤害属性角色五类公式归属；当前仅保存“同角色默认”假设，不会伪装成 Core 已确认事实。

## 0.9.23 - 2026-09-01

- Buff 卡新增“灵可·覆纹追击强化”类型：只修改覆纹附加伤害内部的追击部分，不进入主伤害、普通增伤、浸染或其他环合反应。
- 覆纹统一拆为 `K(H;r,b)=[C(H)-1]+r×C(H)×(1+b)`；普通覆纹取 `r=20%、b=0`，灵可被动完整生效取 `r=30%、b=10%`，即 `KL(H)=1.33C(H)-1`。
- 普通验算将灵可被动作为一个原子 Buff；狂暴溯源可选“拆分验证”，分别检查追击比例改写与追击部分增伤是否失效。
- 结果公式用分色子结构显示“环合基础 + 追击部分 × 追击增伤”，公式检查、公式教学、README 与公式文档同步更新，档案结构升级至 schema 15。

## 0.9.22 - 2026-08-31

- 新增版本化角色技能段数据库：从角色属性表生成 627 条逐段记录，并保留角色、GA、GE、技能分类、描述、1–13 级倍率和多段表达式信息。
- 抓包时间线会优先用唯一 GE 精确对应角色表；无法精确对应时，仅按 `Melee / Skill / UltraSkill / QTE / LTE / Reaction` 建立临时分类，不会擅自套用倍率。
- 抓包窗口新增由玩家填写的技能等级，默认保持未设置；抓包只负责匹配技能段，不会把技能误标为 13 级。玩家明确填写等级后，精确匹配的逐击记录送入常规验算时才会自动填写该等级的单段倍率；现有非零手填倍率不会被覆盖。
- `LTE / 同频合击` 的分类优先于 `QTE / 援护技`，角色表可以覆盖通用事件名分类；3 个重名 GE 会明确保留为歧义候选，等待人工确认。
- 新角色或测试服暂未入表的技能仍可通过事件名、手工动作标记和反向验算继续工作；灵可等跨角色特殊结算规则暂不硬编码。

## 0.9.21 - 2026-08-29

- 抓包接入支持新版 `nte-core` 的 `battle.get_record` / `battle.get_axis`：逐击读取伤害、角色、GA、GE、攻击类型、伤害属性和目标，不再依赖短间隔累计摘要猜测单段伤害。
- 主伤害与追击伤害拆成独立的 `1 HIT` 证据，可分别映射并送入常规验算或狂暴溯源；旧版 Core 自动回退到摘要差值模式。
- 原始事件预览移除固定 PacketHandler 签名 `3` 的旧假设，兼容连接相关的封包前缀，同时继续排除握手包并用连续包序号确认游戏流。
- 抓包界面显示当前采用“逐击读取”还是“摘要兼容”，停止抓包时也会排空最后一页逐击数据。

## 0.9.20 - 2026-08-20

- 修正覆纹结算方式：覆纹不再作为乘区放大技能主伤害，而是对每次可生效的常规伤害追加 `原伤害 ×（当前覆纹倍率 - 1）` 的独立伤害。
- 技能实时预览和常规验算结果会在主伤害旁同步显示覆纹应有的精确值与游戏显示值。
- 狂暴溯源新增覆纹独立伤害候选，可将抓包或手动输入的附加伤害反查到对应技能。
- 公式检查、公式教学、README 与公式文档同步区分“浸染乘算”和“覆纹附加”，档案结构升级至 schema 14。

## 0.9.19 - 2026-08-16

- 修复公式探针被原生公式对话框遮挡的问题；提示卡改为浏览器顶层 Popover，始终浮在公式检查和公式教学窗口之上。
- 保留普通定位降级逻辑，并新增顶层显示状态的桌面窗口校验。

## 0.9.18 - 2026-08-16

- 公式检查与公式教学中的乘区新增悬停/键盘聚焦说明，以 MathML 展示真正带分数线的数学表达式。
- 防御区完整展开角色等级、敌人等级与防御穿透分式；抗性区展示有效抗性和正负抗性的分段函数。
- 环合修正、浸染/覆纹和倾陷上限等除法公式同步改为直观分式，并补充变量含义与模型例外。

## 0.9.17 - 2026-08-16

- 公式编辑与公式教学改为宽屏工作台布局，为常规伤害、浊燃等长公式提供更大的横向展示空间。
- 公式目录和教学公式在窗口空间不足时自动换行，不再要求拖动卡片内部的横向滚动条。

## 0.9.16 - 2026-08-16

- 教学中的“添加候选效果”“添加伤害/治疗技能”“添加倾陷 Buff”“添加贡献角色”和“添加倾陷实测”改为点击后自动进入对应填写步骤，不再要求额外点击“下一步”。
- 首次切换到单人治疗或倾陷伤害时，会自动打开一次对应模式的专属教学；完成或跳过后记住该模式，避免重复打断操作。
- 桌面冒烟测试新增操作驱动跳步和治疗/倾陷首次切换教学校验。

## 0.9.15 - 2026-08-16

- 教学聚光框不再选中 Buff、技能、面板等分区标题，改为指向真实输入框、添加按钮、输入卡片、验算控件和结果卡片。
- Buff、技能、治疗记录、倾陷 Buff、倾陷贡献角色均改为“先高亮添加按钮，再高亮新增/最后一张可见卡片”的两段式教学。
- 教学卡片新增明确的操作提示条；聚光框内的按钮和输入框可以在教学期间直接点击与填写。
- 动态输入目标会选择当前工作区最后一张可见卡片，避免伤害与倾陷记录同时存在时指向隐藏内容。

## 0.9.14 - 2026-08-16

- 修复从“单人治疗”或“倾陷伤害”启动教学后，被强制切回单人伤害并显示错误步骤的问题。
- 操作教学拆分为伤害、治疗、倾陷三条自适应路线；在第 1 步切换计算类型后，后续目标与文案会同步切换。
- 倾陷教学不再出现 OCR 或普通攻击面板，改为讲解敌人预设、倾陷专用 Buff、小队贡献角色、实测合计和逐角色验算。
- 治疗教学改为讲解攻击/治疗面板、OCR、治疗相关 Buff、治疗倍率和治疗验算。
- 从教学结尾或公式页打开公式教学时，会直接进入当前计算类型对应章节。

## 0.9.13 - 2026-08-16

- 新增首次使用聚光灯引导，直接附着于计算类型、角色面板、OCR、Buff、技能、验算、结果和公式入口；完成或跳过后不再自动弹出。
- 顶部新增“使用教学 / GUIDE”，可随时重新播放且不会修改现有输入。
- 新增独立公式教学，按常规伤害、防御与抗性、环合反应、治疗和倾陷小队五章说明乘区与例外，并与结果公式追踪共用颜色语言。

## 0.9.12 - 2026-08-10

- 倾陷 Buff 与常规伤害/治疗 Buff 完全分离，分别显示、搜索和保存，避免跨模式误用个人乘区。
- 倾陷抗性削弱改为按属性汇总：同属性角色触发的不同减抗来源可以叠加并共享；无视抗性与防御穿透仍严格逐角色计算。
- 倾陷自定义 Buff 范围中，个人乘区只作用勾选角色，减抗自动扩展给所有同属性贡献者。
- OCR 停止识别和导入环合强度；环合反应改为提示手动填写反应双方非战斗面板环合强度较高的一方。
- 档案结构升级至 schema 13，旧倾陷档案会把原共享 Buff 复制到新的倾陷专用 Buff 区以便继续核对。

## 0.9.11 - 2026-08-09

- 抗性区改为按有效抗性分段计算：非负时使用 `1 - 有效抗性`，负值时使用 `1 - 有效抗性 / (1 - 有效抗性)`。
- 有效抗性统一定义为“敌人原始抗性 - 无视抗性 - 抗性削弱”，角色面板、Buff 与倾陷贡献均可分别录入无视抗性。
- 公式检查、结果乘区提示、档案结构与说明文档同步升级至 schema 12。

## 0.9.10 - 2026-08-07

- 修正浸染与覆纹的独立倍率为 `1.2 × [1 + (0.2 × 最终环合强度) / (180 + 最终环合强度)]`。
- 公式检查页将旧“基础增伤 / 环合除数”改为“基础倍率 / 环合系数 / 环合常数”，旧版自定义公式常量会回落到新公式默认值。
- 同步更新公式追踪、README、公式文档、桌面自检与视频介绍 PPT。

## 0.9.9 - 2026-08-06

- 顶部计算类型改为“单人伤害 / 单人治疗 / 倾陷伤害”三工作区；倾陷不再挤在普通伤害双栏中。
- 倾陷工作区隐藏无关的单人角色面板，并让倾陷小队与 Buff 分区横向占满；角色输入卡改为自适应换列，修复窄窗口内容出界。
- 每条伤害与治疗记录新增实时理论值小字：默认所有参与搜索/锁定 Buff 生效，自动叠层取最大层、固定叠层按填写层数，挂起 Buff 不参与。
- 普通技能与倾陷记录在界面和验算数据源中相互隔离，切换工作区时保留各自输入；存档结构升级至 schema 10。
- 倾陷角色的旧“特殊乘区”改为“倾陷专属特殊增伤（%）”，默认填写 0、实际乘区为 `1 + 输入值`；明确不读取浸染或覆纹，并兼容旧档案中倍率 1 的数据。

## 0.9.8 - 2026-08-06

- 新增倾陷伤害模型：按小队角色分别使用等级、属性抗性、防御穿透、抗性削弱、倾陷增伤与特殊乘区结算，精确贡献求和后统一四舍五入且不可暴击。
- 内置“塞润尼缇（高危6）”预设：敌人等级 90、倾陷上限区 `70 / 3`、咒抗性 16%、其余六属性抗性 20%；80 级单人固定基础值为 3603。
- 新增倾陷小队贡献区，以及 Buff 独立的“倾陷作用角色”范围；同一 Buff 可只影响指定角色。
- 倾陷增伤字段只在存在倾陷技能时显示并进入搜索，普通增伤、技能倍率、持续强化、暴击、环合及浸染/覆纹均不会产生无意义组合。
- 公式检查页新增倾陷基础值和上限除数，存档结构升级至 schema 9，并继续兼容旧存档。

## 0.9.7 - 2026-08-05

- 按已启用技能的公式白名单和 Buff 作用范围裁剪搜索原子；持续伤害验算不再排列该公式未读取的 Buff 属性。
- 常规伤害与治疗验算固定基础区，只反推 Buff；狂暴溯源才允许相关基础区失效，并继续优先基础区正常的解释。
- Buff 卡片新增“参与搜索 / 锁定生效 / 挂起排除”三态控制；锁定是高触发优先的绝对约束，挂起不参与计算但保留输入。
- 锁定的自动叠层 Buff 只搜索 `1～最大层数`，固定层数最低为 1；存档结构升级至 schema 8。

## 0.9.6 - 2026-08-05

- 将旧“浸染 / 特殊独立倍率”输入改为互斥的“未启用 / 浸染 / 覆纹”选项；选择后在 Buff 区显示独立规则卡片。
- 浸染与覆纹统一按独立倍率 `1 + 20% + 最终环合强度 / 1400` 计算，并继续只作用于常规伤害模型。
- 浸染 / 覆纹会读取每个验算假设中实际生效的环合强度百分比与固定数值 Buff，而不是只读取基础面板值。
- 公式检查页新增浸染 / 覆纹基础增伤与环合除数，存档结构升级至 schema 7，并兼容载入旧版浸染 / 特殊乘区开关。

## 0.9.5 - 2026-08-05

- 修正浊燃固定基础值为 `2700`；继续读取防御、抗性、环合修正、持续伤害强化与暴击。
- 新增黯星环合反应：固定基础值 `45000`，防御乘区固定为 `1`，读取抗性、环合修正、暴击以及自定义点名增伤。
- 新增“公式 / FORMULA”检查与调整页，可修改三类环合反应基础值、环合除数与浸染系数，并查看各模型乘区白名单。
- 逐项验算新增分色乘区轨道，直接展示本次理论值实际相乘的因子和值。
- 环合反应默认排除攻击、普通增伤、技能倍率、浸染和特殊独立；只有作用范围为“自定义技能”且明确勾选对应反应的增伤可以覆盖默认排除。
- 基础面板环合强度严格限制为 `0～360`，Buff 结算后的最终环合强度不设上限。

## 0.9.4 - 2026-08-05

- 修正角色筛选把 Core 运行状态一并隐藏、造成“审计无数据”假象的问题，并明确区分 Core 主动通知与 PCAP 原始名称扫描。
- 原始事件现在分别显示名称重组所需的 bit 位移与 UDP 载荷字节数，避免把 `4b` 误解为四字节字段。
- `Default__Buff_*` 即使名称包含 `Skill` 或 `Ultra` 也优先归为效果，避免错误归入动作。

## 0.9.3 - 2026-08-05

- 抓包区新增“Core 通知审计”，展示解析核心实际输出的方法名、参数结构、内部标识与标量样本；可沿用角色关键词聚焦 `Zankou` 等任意内部名称。
- 每次抓包自动生成精简 NDJSON 审计日志，隐藏 payload、网络端点与身份字段；战斗摘要默认隐藏，避免覆盖未知 Effect 通知。
- 登录测试服样本已发现 `Default__Buff_Zankou_Passive1_C`、`Passive2`、`ListenBattle` 与 `MagicDotDamCritUp` 等稳定 Buff 类资产名，为后续索引学习保留依据。

## 0.9.2 - 2026-08-04

- 将 `AbilitySystemComponent` 从普通系统字段调整为默认显示的“伤害载体”，明确它承载伤害同步但不代表技能来源。
- 原始事件预览支持选择一次匹配记录、映射到技能，并直接送入常规验算或狂暴溯源；不再依赖标准战斗摘要必须有数据。
- 支持用 `Sagiri` 或 `GA_Sagiri_*` 形式检索角色事件，并把相邻 ASC 跳字载体关联到最近的角色 GA 名称。

## 0.9.1 - 2026-08-04

- 修复测试服真实事件流因 PacketId 确认条件过严而不显示的问题；现在可由多个高置信度 NTE/UE 事件特征共同确认数据流。
- 抓包时显式使用 Core 检测出的推荐网卡，并恢复接收服务端返回封包。

## 0.9.0 - 2026-08-04

- 抓包工具新增测试服“原始事件预览”：从连续游戏 UDP 流中按毫秒列出 `Melee`、`Skill`、`Reaction`、`CritDamage`、`GA` / `GE` 等可读内部名称，并支持一键复制。
- 可填写游戏显示伤害，按事件前后 0.35 秒内的候选数值进行 `±20` 匹配和误差排序；候选值明确不作为已确认伤害、暴击或技能段数。
- Core 改为显式启用原始 PCAP，计算器只增量读取完整 PCAPNG 块，不显示载荷或网络端点；README 补充本机存储位置、磁盘占用和禁止公开上传原始抓包的说明。
- 事件发现只接受具备连续 UE 包序号的流，后台 UDP 不会直接进入预览；真实 148 KB 测试服样本可稳定发现两组 `Melee1 / Melee2` 与 `CritDamageBase`，重复轮询不重新扫描旧数据。

## 0.8.0 - 2026-08-04

- Windows 桌面版新增实验性“抓包 / EVENT LEARN”入口，可连接用户自行提供的标准 `nte-core.exe`，自动启动/停止本机 Npcap 采集并接收累计战斗摘要。
- 将累计技能摘要转换为新出现的伤害增量，显示时间、动作标记、来源类别、GA/GE 名称、伤害值及 Hit 数；最多保留 300 条前端事件和 500 条待取增量。
- 只有单次增量恰好为 `1 HIT` 时才允许写入常规验算或狂暴溯源；多段短间隔合并值明确标记为合计并禁止误填为单段伤害。
- 新增按“角色名称 + 版本”保存的事件映射，可把未知来源连接到现有技能、新建常规技能、创生花或浊燃；重复实测会复制目标技能倍率，便于逐条验证。
- 参考上游分类规则，将 `Reaction_1` 事件作为创生花候选、`Reaction_5` 事件作为浊燃候选；测试服未知命名仍允许人工覆盖。
- 明确标准 Sidecar 不提供逐击、暴击标记和 Buff apply/remove/stack 事件；界面不伪造这些字段，并为后续逐包事件适配器预留 Buff 区域。
- 解析核心启动失败会自动清理辅助进程，停止抓包或关闭计算器也会执行关闭；显式禁用原始 PCAP 文件写入，且发行包不捆绑第三方 AGPL 组件。
- README 新增抓包环境、事件学习、单段/多段规则、许可证边界和隐私说明，并加入事件时间线截图。

## 0.7.0 - 2026-08-04

- 修正浊燃伤害：固定视为持续伤害，并读取独立的持续伤害强化乘区；持续伤害强化同时支持普通持续伤害、Buff 生效/失效排查和叠层反推。
- Buff 行新增“高触发优先”偏好及按全部、持续、非持续或自定义技能设置作用范围；偏好只用于同等误差解释排序，不缩减搜索空间。
- 常规技能新增“持续伤害”Tag，公式不变，并可作为 Buff 作用范围条件。
- 新增可暴击的“浊燃伤害”：`2100 × 防御区 × 抗性区 × (1 + 最终环合强度 / 600)`，不读取攻击、常规增伤、技能倍率、浸染或特殊独立乘区。
- 创生花和浊燃均明确使用固定基础倍率 `1`，选择这两种模型时隐藏无效的基础倍率输入框。
- 新增可选叠层 Buff：支持每层数值、最大 30 层、自动反推 `0～最大层数` 和固定层数；普通 Buff 默认逻辑保持不变。
- 自动层数作为互斥离散维度参与完整枚举或限宽搜索，同一 Buff 的所有属性共享层数；乘算技能倍率按 `1 + 每层提升 × 层数` 计算而非逐层复利。
- 重写仓库 README，按下载、四步工作流、使用场景、字段、公式和构建发布重新组织，并更新总览、验算、溯源、治疗、OCR、Buff 作用范围及叠层反推八张界面截图。
- 新增可复用的离线 README 截图生成脚本，使用本机 Edge 自动构造各模式示例状态。
- 以前端设计规范重塑界面视觉系统：加入紧凑品牌栏、四段验算工作流、测量刻度式分区标题、统一数据字体及更明确的激活与状态反馈。
- 修复 pywebview 递归扫描公开窗口对象导致桌面版启动无响应的问题，并恢复 WebView2 GPU 渲染。
- 暴击伤害统一按游戏面板原值填写和 OCR 导入；计算时自动补上基础倍率 `1`，例如面板 `134%` 按 `2.34` 倍计算。
- 项目更名为“Damage Bug Finder for NTE / 异环NTE数值验算器”，并使用新的自定义 Windows 应用图标。
- 强化界面分区层级：面板、效果、技能、验算操作与结果区域采用统一的轻量卡片边框、标题栏和克制的主色强调。
- 将基础伤害乘区归入角色与目标面板，将候选效果区更名为 Buff 区并补充来源提示，同时增加角色归档栏顶部留白。
- 重新对齐计算类型、角色档案操作和验算工具栏；宽屏使用稳定网格，窄窗口按明确断点整齐换行。

## 0.5.2 - 2026-07-20

- 桌面版改为直接加载计算器，不再通过受限 iframe 和消息桥转发桌面 API。
- 移除构建工具自动加入的在线图标脚本，避免离线环境等待网络导致页面迟迟无法完成加载。
- 桌面环境直接显示 OCR 入口，不再依赖异步桥接成功后才出现。
- WebView2 默认使用软件兼容渲染，规避游戏、显卡叠加层或驱动状态引起的 GPU 初始化阻塞。
- 新增桌面页面结构与 OCR 入口的启动自检。

## 0.5.1 - 2026-07-20

- 新增无需每次解压运行环境的快速启动目录版，推荐日常测试使用。
- OCR 与图像组件改为点击“自动读取游戏面板”后才加载，降低普通启动负担。
- 当前数据已经处于待验算状态时，不再随每次键盘输入重复清空和重排结果区。
- 保留单文件版作为便携备选，并在 README 中说明两种封装的启动差异。

## 0.5.0 - 2026-07-19

- 桌面版加入游戏窗口自动捕获，无需手动保存或选择日常测试截图。
- 截图倒计时期间自动隐藏计算器并切回游戏，允许打开或滚动属性详情。
- 使用 Windows 本机简体中文 OCR，识别过程完全离线且不保存完整截图。
- 支持多张滚动面板合并、重复值交叉验证、冲突提示和确认后填表。
- 自动填写总攻击、基础攻击、角色等级、暴击伤害、通用/属性增伤、环合强度与治疗加成。
- 七种属性伤害增强统一映射到属性增伤，并排除属性伤害抗性行。
- 保留多图手动导入作为备用入口，HTML 版继续保持纯静态计算功能。
- 桌面 EXE 自检新增离线 OCR 回归入口。

## 0.4.1 - 2026-07-19

- 将非战斗面板环合强度限制为 `0～360`。
- 觉醒、武器等百分比和固定提升仍可使最终环合强度超过 360。
- 载入旧配置时自动把超出范围的基础环合强度收敛到合法范围。

## 0.4.0 - 2026-07-19

- 将伤害计算与治疗计算改为独立界面模式，自动隐藏无关输入区。
- 治疗公式改为“战斗面板攻击 × 治疗倍率 ×（1 + 治疗加成）”。
- 加入基础治疗加成及觉醒、武器等候选治疗加成的生效/失效排查。
- 加入环合强度、百分比提升和固定数值提升。
- 加入仅作用于常规伤害的浸染乘区与可选特殊独立乘区。
- 创生花公式改为“9000 × 防御区 × 抗性区 ×（1 + 环合强度 / 600）”。
- 明确浸染和特殊独立乘区不作用于创生花。
- 更新 README 的游戏内字段位置、敌人抗性数据来源、公式说明和 Git 上传清单。
- 加入 MIT License。

## 0.3.0 - 2026-07-19

- 加入标准、深度和极限三档搜索强度。
- 完整枚举上限分别扩展到 18、20 和 22 个判断项。
- 完整枚举改为在线保留最优 300 个解释，避免结果数组随状态数无限增长。
- 大规模限宽分别扩展到 5,000、20,000 和 50,000 条路径。
- 搜索强度随角色和版本档案一并保存。
- 重写 README，加入实机截图、逐字段填写规则、完整结果解释和性能边界。

## 0.2.0 - 2026-07-19

- 加入可直接运行的 Windows 桌面 EXE。
- 桌面版使用固定应用数据目录，角色和版本保存不受临时解压路径影响。
- 将“最接近的效果组合”改名为“最符合实测的全局状态”。
- 将“范围内候选”改名为“±20 内来源数”，并补充验算结果解释。
- 备选全局状态只显示整批零误差结果，带误差的近似状态不再列出。
- 加入“清空当前”按钮，清除当前输入和草稿但保留已保存档案。

## 0.1.0 - 2026-07-19

- 加入常规技能、创生伤害和治疗计算。
- 支持技能倍率加算与乘算。
- 支持多个伤害技能和治疗技能。
- 加入角色与版本配置保存和草稿记忆。
- 加入基础乘区、觉醒和武器效果失效排查。
- 加入其他技能倍率误用检测。
- 加入 ±20 单项误差过滤。
- 加入完整枚举和大规模限宽搜索。
- 加入狂暴溯源模式和批量未知伤害匹配。
- 加入宽屏布局和技能、结果分页。
